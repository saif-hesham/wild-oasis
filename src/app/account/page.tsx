import React from 'react'
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Account',
};

const Page = () => {
  return (
    <div>Account</div>
  )
}

export default Page